# 26-06-09-DL


## CNN층을 활용한 동물 사진 데이터 분류 실습

### 데이터 불러오기
- 로컬에서 돌아가는 아나콘다 같은 경우에는 경로를 사용할 때 따로 연결을 해 줄 필요가 없지만 Colab을 사용할 경우에는 내 경로를 먼저 연결한 후에 내 폴더 안에 있는 파일들을 사용할 수 있다.

### 연결 방법
1. 드라이브 마운트
- 내 드라이브 연결
2. 작업 디렉토리(위치)를 사용할 폴더로 변경
- %cd 사용

``` python
# 드라이브 마운트
from google.colab import drive
drive.mount('/content/drive')

# 작업 디렉토리 변경
 %cd /content/drive/MyDrive/Colab Notebooks/DL
```

### 압축 풀기
- data 폴더 안에 있는 small_animal.zip에 대해 압축을 풀도록 명령을 내리기

``` python
# 1. Colab 현재 작업폴더에 data 폴더 만들기
!mkdir -p ./data

# 2. 구글드라이브에 있는 zip 파일을 ./data로 복사
!cp "/content/drive/MyDrive/data/animal_small.zip" ./data/

# 3. 원하는 방식으로 압축 풀기
!unzip ./data/animal_small.zip -d ./data/animal
```

### 데이터 사용
- 압축을 해제한 데이터를 사용하기
- 우리가 사용할 데이터는 이미지 데이터인데 이미지 데이터는 그대로 사용할 수 없고 사용할 수 있도록 수치화하여 사용해야 한다.
    - opencv에서 제공하는 이미지 읽어오는 기능을 사용하기
    - tensorflow에서 제공하는 image_dataset_directory 사용하기


``` python
# 데이터 다루기 위한 도구 불러오기
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt

# 이미지를 수치화하기 위해 폴더에 접근하여 이미지를 읽어올 수 있도록 도와 주는 도구 불러오기
from tensorflow.keras.utils import image_dataset_from_directory

# 반복문을 사용할 때 진행률 확인하기 위한 도구 불러오기
from tqdm import tqdm

# 이미지 읽어오기(수치화 진행하기)
train_data = image_dataset_from_directory(
    # 접근 경로 설정 (이미지들이 있는 폴더를 설정)
    directory = './data/animal/animal_small',
    labels = 'inferred', # 폴더 이름을 그대로 정답 데이터로 사용
    # 다중 분류에 맞도록 정답 형태를 지정(원핫인코딩 자동으로 진행)
    label_mode = 'categorical',
    # 색상채널을 설정(RGB 형태의 채널을 설정)
    color_mode = 'rgb',
    #이미지의 크기를 전부 동일하게 설정(이미지의 크기가 다르면 학습이 불가능)
    image_size = (224,224)
)

# 문제 데이터와 정답 데이터 분류하기
X_train =[]
y_train =[]

# tqdm: 반복문을 사용할 때 진행률 확인이 가능하도록 설정
# as_numpy_iterator(): 배열 형태로 데이터를 나눠서 사용할 수 있도록 설정
for img, label in tqdm(train_data.as_numpy_iterator()):
    X_train.append(img)
    y_train.append(label)


# 리스트에 데이터가 담겨 있기 때문에 배열 형태로 변경하기
# concatenate: 하나씩 하나씩 선택하여 배열 형태로 변경하기(연결)
X_train = np.concatenate(X_train)
y_train = np.concatenate(y_train)

# 데이터 크기 확인
print(X_train.shape)
print(y_train.shape)
```

### MLP층으로 인공신경망 설계
- activation : relu / optimizer : adam

``` python
# 라이브러리 불러오기
from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import Dense, Flatten, InputLayer

# Sequential: 뼈대 생성
# Dense : 중간층, 출력층 쌓을 때 사용
# Flatten : 고차원의 데이터를 1차원으로 펴는 도구
# InputLayer : 입력층을 쌓을 때 사용하는 도구 중 하나

# 신경망 설계

# 뼈대 생성
mlp_model = Sequential()

# 입력층 쌓기
mlp_model.add(InputLayer(shape = (224,224,3))) # 3차원의 데이터 입력
# MLP층을 사용하기 위해 Flatten을 이용하여 1차원으로 데이터 변경
mlp_model.add(Flatten())

# 중간층 쌓기
mlp_model.add(Dense(units = 128, activation = 'relu'))
mlp_model.add(Dense(units = 256, activation = 'relu'))
mlp_model.add(Dense(units = 128, activation = 'relu'))

# 출력층 쌓기
mlp_model.add(Dense(units = 4, activation = 'softmax'))

# 학습 방법 및 평가 방법 설정
mlp_model.compile(
    loss = 'categorical_crossentropy',
    optimizer = 'adam',
    metrics = ['accuracy']
)

# 학습 진행
mlp_model.fit(
    X_train, y_train,
    epochs = 20,
    validation_split = 0.2
)

# 결과 시각화
plt.figure(figsize = (15,5))
plt.plot(mlp_model.history.history['accuracy'], label = 'acc')
plt.plot(mlp_model.history.history['val_accuracy'], label = 'val_acc')
plt.legend()
plt.show()
```

### CNN 모델
- MLP 신경망으로 이미지를 처리할 때 이미지의 위치 또는 크기에 따라서 매우 민감하게 처리되어 비슷한 크기와 위치를 맞추었을 때만 제대로 된 예측이 가능하다.
- 실제 이미지는 정해진 크기나 위치가 없기 때문에 MLP만으로는 이미지 분석을 잘 할 수가 없다.
- 새로운 방안을 고안하여 나온 방법이 바로 CNN층이다.
- CNN층은 이미지 데이터에서 특징값을 추출하여 비슷한 데이터를 골라낼 수 있도록 설정한 방법

### CNN 원리
- 이미지의 특징을 추출하기 위해 **필터(커널)**을 넣어서 합성곱 계산의 특징을 추출하는 방법
- 이미지 전체에 동일한 연관성으로 처리하는 대신 특정 범위에 한정해 처리하면 효과적으로 분석할 수 있다는 방법을 고안하여 만들어진 방법

### CNN에서 중요한 용어 정리
- 패딩(Padding)
    - 필터의 크기로 인하여 가장자리 부분의 데이터는 분석이 어렵기 때문에 이를 보안하기 위해 입력 데이터의 가장자리에 0을 미리 채워넣어 데이터의 손실을 방지하고 가장자리 데이터 또한 분석을 하도록 설정한 방법
- 축소 샘플링
    - 데이터의 크기가 너무 크기 때문에 연산을 너무 많이 수행하여 시간이 오래 걸리기 때문에 불필요한 데이터까지 넘길 이유가 없어 합성곱 결과에서 모든 정보를 전달하지 않고 일부만 샘플링 하여 넘겨 주는 기법
    - 스트라이드
    - 풀링

### 축소 샘플링
- 스트라이드(Stride)
    - 축소 샘플링 방법 중 하나로 합성곱 연산을 할 때, 모든 데이터를 분석하는 것이 아니라 2~3픽셀씩 옆으로 건너 뛰면서 연산하는 방법
    - 이미지의 크기가 너무 커서 자원 소모를 방지할 때 사용
    - 너무 많은 픽셀을 이동하면 중요한 데이터를 건너뛰어 버리는 현상이 일어날 수 있기 때문에 적당한 픽셀을 이동해야 한다.
- 풀링(Pooling)
    - 합성곱 연산의 결과를 다음 층으로 넘길 때, 모든 데이터를 넘기는 것이 아니라 대표값(가장 큰 값 혹은 평균값)을 넘기는 방법
    - 대표 정보를 넘기기 때문에 불필요한 데이터는 제거하고 중요한 데이터만 남겨 활용할 수 있어 효율적으로 학습할 수 있다는 특징이 있음

### CNN 신경망 설계
``` python
# CNN층을 쌓기 위한 도구 불러오기
from tensorflow.keras.layers import Conv2D
from tensorflow.keras.layers import MaxPooling2D

# Conv2D: 이미지의 특징을 잡아내도록 도와 주는 층(이미지 커널 계산 진행)
# MaxPooling2D: 불필요한 데이터를 삭제하여 특징만 전달하도록 하는 풀링층

# 2D: 2차원의 합성곱 계산 -> 연산이 빠르다
# 3D: 3차원의 합성곱 계산 -> 연산이 느리지만 상세한 분석이 가능

# 신경망 설계
# CNN층은 결과 출력을 하는 것이 아니라 특성을 추출해 주는 역할이기 때문에 MLP층과 혼합하여 사용해야 한다.
# 뼈대 생성 -> 입력층 -> CNN층 -> MLP층

# 뼈대 생성
cnn_model = Sequential()

# 입력층 쌓기
cnn_model.add(InputLayer(shape = (224, 224, 3,)))

# CNN층 쌓기
cnn_model.add(Conv2D(
    # 필터(커널) 개수 설정
    filters = 64,
    # 필터의 크기 조절(무조건 홀수로 사용), 일반적으로 (3,3) or (5,5) 사용
    kernel_size = (3, 3),
    activation = 'relu'
))

# Pooling층 쌓기
cnn_model.add(MaxPooling2D())

cnn_model.add(Conv2D(
    filters = 128,
    kernel_size = (3,3),
    activation = 'relu'
))
cnn_model.add(MaxPooling2D())

cnn_model.add(Conv2D(
    filters = 64,
    kernel_size = (3,3),
    activation = 'relu'
))
cnn_model.add(MaxPooling2D())

# MLP층 쌓기
# 특성추출부에서 추출한 특성값을 이용하여 어떤 동물의 이미지인지를 판단하는 MLP층을 쌓기
# 1차원으로 데이터 펴 주기
cnn_model.add(Flatten())

# 중간층 쌓기(위의 MLP모델과 동일하게 층 설정)
cnn_model.add(Dense(units = 128, activation = 'relu'))
cnn_model.add(Dense(units = 256, activation = 'relu'))
cnn_model.add(Dense(units = 128, activation = 'relu'))

# 출력층 쌓기
cnn_model.add(Dense(units = 4, activation = 'softmax'))

# 학습 방법 및 평가 방법 설정
cnn_model.compile(
    loss = 'categorical_crossentropy',
    optimizer = 'adam',
    metrics = ['accuracy']
)

# CNN 모델 학습
cnn_model.fit(
    X_train, y_train,
    epochs = 20,
    batch_size = 32,
    validation_split = 0.2
)

plt.figure(figsize = (20, 8))

# MLP 모델 출력
plt.plot(mlp_model.history.history['accuracy'], label = 'MLP_acc')
plt.plot(mlp_model.history.history['val_accuracy'], label = 'MLP_val_acc')

# CNN 모델 출력
plt.plot(cnn_model.history.history['accuracy'], label = 'CNN_acc')
plt.plot(cnn_model.history.history['val_accuracy'], label = 'CNN_val_acc')

plt.legend()
plt.show()
```

### 결과 확인
- 일반적인 MLP를 사용할 때는 성능이 매우 낮았다. 하지만 CNN층을 사용해 보니 성능이 향상된 것을 알 수 있음
- 이미지 데이터 같은 경우에는 비슷한 형태나 방향, 각도, 위치 등을 맞추기 어렵기 때문에 CNN층과 같이 특징(특성)을 추출할 수 있는 Layer를 추가하는 것이 성능 향상에 도움이 됨
- 현재 데이터의 양이 많지 않으며, 층을 쌓을 때 사용법 위주로 쌓았기 때문에 과적합이 일어난 것을 알 수 있음
- 앞으로 해야 할 일은 과적합을 방지하기 위해 데이터를 증강하거나 층을 더욱 상세히 쌓아 과적합을 방지해야 할 것 같음
- 또한, 전처리에 대해 자세히 한 것은 아니기 때문에 전처리 또한 도움이 될 것으로 판단이 됨

### 스케일링
- 데이터 전처리 방법 중 하나로 데이터의 범위값을 일정하게 변경하여 데이터의 범위에 따라서 해당 컬럼이 중요하게 여겨지는 것을 방지하는 전처리 기법

## 전이학습 사용하기
### 전이학습
- 전이학습이란 사전 학습된 모델의 지식을 새로운 다른 데이터에 적용하는 방법
    - 모델이 이미 학습한 일반적인 지식을 기반으로 더욱 빠르고 효과적인 학습이 가능하다.
    - 적은 양의 데이터로 새로운 데이터에 대한 학습을 가속화하며 성능을 향상 시키는데 유용
    - 자연어 처리, 컴퓨터 비전 등 다양한 분야에서 사용되고 있다.
    - 사전 학습된 모델의 일부 또는 전체를 사용하는 방법

### 전이학습의 종류
- 특성추출방식
    - CNN층에서 특성 추출부만을 가져와 사용하는 방식으로 MLP층만 내 데이터에 맞도록 다시 설계해 사용하는 방법, CNN층은 그대로 사용
    - 사전학습된 데이터와 사용할 데이터의 특성의 차이가 클 경우에는 성능이 나지 않을 수 있음
- 미세조정방식
    - 기존에 학습된 모델을 기반으로 새로운 목적에 맞게 변형하고 이미 학습된 모델의 '가중치'를 미세하게 조정 시키는 방식
    - CNN층의 일부분을 내 데이터에 맞도록 변형 시켜서 사용하는 방법

``` python
# 드라이브 마운트
from google.colab import drive
drive.mount('/content/drive')

# 작업 디렉토리(위치) 변경

%cd /content/drive/MyDrive/Colab Notebooks/DL
```

### 데이터 불러오기
- 압축을 풀어서 사용했던 동물 데이터 그대로 사용
``` python
# 도구 불러오기
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt

from tensorflow.keras.utils import image_dataset_from_directory
from tqdm import tqdm

# 데이터 불러오기
train_data = image_dataset_from_directory(
    directory = './data/animal/animal_small',
    labels = 'inferred',
    label_mode = 'categorical',
    color_mode = 'rgb',
    image_size = (224,224)
)

# 데이터를 사용할 수 있는 형태로 변경
X_train = []
y_train = []

for img, label in tqdm(train_data.as_numpy_iterator()):
 X_train.append(img)
 y_train.append(label)

#배열을 연결
X_train = np.concatenate(X_train)
y_train = np.concatenate(y_train)

# 데이터 크기 확인
print(X_train.shape)
print(y_train.shape)
```

### VGG16
- CNN 아키텍처로 획기적인 객체 인식 모델을 기초로 ImageNet에서 높은 정확도를 보이는 모델

### VGG16 구조
- 13개의 Convolution Layer로 구성된 CNN 아키텍처 모델
- 모든 Convolution Layer의 커널 크기는 (3,3)로 이루어져 있고, 함수는 relu를 사용
- 입력 이미지의 크기는 (224, 224, 3)의 컬러 이미지를 사용


``` python
# VGG16 모델 로드
from tensorflow.keras.applications import VGG16

# 모델 가져오기
vgg16 = VGG16(
    include_top = False, # MLP층을 사용하지 않도록 설정
    weights = 'imagenet', # imagenet으로 학습된 정보를 사용(가중치 값)
    input_shape = (224, 224, 3) # 입력할 데이터의 크기(입력층)
)

# 특성 추출 방식을 사용하기 위해 CNN 부분은 그대로 유지하도록 동결 설정하기
# 동결: 기존에 학습된 정보를 변경 시키지 않고 그대로 사용하도록 내 데이터가 추가 학습이 되지 않게 설정하는 방법

# 동결 설정
vgg16.trainable = False # 학습을 할지 하지 않을지 유무 설정
#trainable = False -> 동결 설정(추가로 학습하지 않도록 설정)

# 전이 학습을 위해 인공신경망 설계
from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import Dense, InputLayer, Flatten

# 특성 추출 방식 모델 설계

# 뼈대 생성
transfor_model = Sequential()

# vgg16 모델을 내 모델에 추가
transfor_model.add(vgg16)

#  ------------분류기(MLP층) 설정------------
transfor_model.add(Flatten()) #3차원의 데이터를 1차원으로

# 중간층 쌓기
transfor_model.add(Dense(units = 64, activation='relu'))
transfor_model.add(Dense(units = 128, activation='relu'))
transfor_model.add(Dense(units = 64, activation='relu'))

# 출력층 쌓기
transfor_model.add(Dense(units = 4, activation='softmax'))

# 학습 방법 및 평가 방법 설정
transfor_model.compile(
    loss = 'categorical_crossentropy',
    optimizer = 'adam',
    metrics = ['accuracy']
)

# 학습 진행
transfor_model.fit(
    X_train, y_train,
    epochs = 20,
    validation_split = 0.2
)

# 시각화
plt.figure(figsize = (15,5))
plt.plot(transfor_model.history.history['accuracy'], label = 'acc')
plt.plot(transfor_model.history.history['val_accuracy'], label = 'val_acc')
plt.legend()
plt.show()
```

### 미세조정 방식 사용
- CNN층 하단의 일부분 층을 내 데이터로 학습 시켜 사용하는 방법

``` python
# 모델 정보 확인
# model.summary()

vgg16.summary()
```

### 정보 확인
- 하단의 block5.conv3층을 활용하여 미세 조정 진행

``` python
# 마지막 층인 block5_conv3층을 학습 시키고 그 외 모든 층은 동결 설정

# 새로운 모델 설계 진행
transfor_model2 = Sequential()

# vgg16을 추가하기 전에 학습이 진행될 층을 제외한 나머지 층은 동결되도록 설정을 진행
# 미세 조정 설정-------------------------------------------------------------------

for layer in vgg16.layers:
    if layer.name == 'block5_conv3':
        layer.trainable = True # 학습이 가능하도록 설정
    else :
        layer.trainable = False # 동결 설정

#----------------------------------------------------------------------------------
# 설정한 vgg16 모델 추가
transfor_model2.add(vgg16)

transfor_model2.add(Flatten())

# MLP층 쌓기 ----------------------------------------------------------------------
transfor_model2.add(Dense(units = 64, activation = 'relu'))
transfor_model2.add(Dense(units = 128, activation = 'relu'))
transfor_model2.add(Dense(units = 64, activation = 'relu'))

# 출력층
transfor_model2.add(Dense(units = 4, activation = 'softmax'))

# 모델 확인
transfor_model2.summary()

# 컴파일
transfor_model2.compile(
    optimizer = 'adam',
    loss = 'categorical_crossentropy',
    metrics = ['accuracy']
)

# 학습
history2 = transfor_model2.fit(
    train_data,
    epochs = 20
)

# 정확도 시각화

plt.figure(figsize=(8, 5))
plt.plot(history2.history['accuracy'], label='train_acc')
plt.legend()
plt.show()

# 손실값 시각화
plt.figure(figsize=(8, 5))
plt.plot(history2.history['loss'], label='train_loss')
plt.legend()
plt.show()
```